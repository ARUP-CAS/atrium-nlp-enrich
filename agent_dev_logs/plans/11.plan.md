## Action Plan: #11 - NameTag3 Multilingual Base Integration

**Primary Goal:** Complete the integration of the NameTag3 multilingual model into the `atrium-nlp-enrich` pipeline based on successful synthetic data tests. *(Status: Completed)*

**Action Items (Completed):**

* [x] **Code Integration (`ne-conllu`):**
  * Update the codebase to call the generalized NameTag3 multilingual models instead of defaulting to per-language variants. *(Achieved via updating `config_api.txt`)*
    * Integrate the model specifically into the `ne-conllu` processing workflow.

* [x] **Tagset Mapping:**
  * Implement the tagset conversion logic (CNEC -> ONTO) validated during the synthetic data testing phase. *(Achieved by implementing `CNEC_TO_ONTO_MAP` in `api_util/summarize_nt_udp.py`)*

* [x] **Output Generation:**
  * Update the output handlers to generate the resulting CSV files.
  * Ensure that the final CSV correctly formats and maps the new entity descriptions resulting from the multilingual model. *(Achieved by refactoring `get_ne_explanation` to accurately parse native ONTO outputs and fallback mapping)*

* [x] **Unit Testing:**
  * Create dedicated assertions in `tests/test_summarize_nt_udp.py` to lock in correct behaviors for native ONTO tags, legacy CNEC tags, compound strings, and empty/unknown tag fallbacks.