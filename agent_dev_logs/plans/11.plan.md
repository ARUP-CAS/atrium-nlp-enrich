## Action Plan: #11 - NameTag3 Multilingual Base Integration

**Primary Goal:** Complete the integration of the NameTag3 multilingual model into the `atrium-nlp-enrich` pipeline based on successful synthetic data tests.

**Action Items:**

1. **Code Integration (`ne-conllu`):**
   * Update the codebase to call the generalized NameTag3 multilingual models instead of defaulting to per-language variants.
     * Integrate the model specifically into the `ne-conllu` processing workflow.

2. **Tagset Mapping:**
   * Implement the tagset conversion logic (CNEC -> ONTO) validated during the synthetic data testing phase.

3. **Output Generation:**
   * Update the output handlers to generate the resulting CSV files.
   * Ensure that the final CSV correctly formats and maps the new entity descriptions resulting from the multilingual model.