# Strategy — Issue #9: TEITOK output: image files dependence handling

> Issue [\#9](https://github.com/ufal/atrium-nlp-enrich/issues/9) · Labels: `enhancement`, `development` · **Status: ✅ Closed**

## Context
The pipeline accurately calibrates TEITOK `bbox` coordinates and facsimile alignments against source page images. The codebase successfully abstracted the coordinate geometry logic into a unified module (`bbox_scale.py`), resolved P0 threading regressions in `summarize_nt_udp.py` that were blocking batch runs, and removed hardcoded fallback dependencies (such as forcing `.png` extensions). The full resolution was integrated in release v0.15.0.

## Status Snapshot
| Item                                              | State      | Note                                                                                          |
|---------------------------------------------------|------------|-----------------------------------------------------------------------------------------------|
| 3-Tier priority scale map implementation          | ✅ **DONE** | Integrated and tested fallback paths (Image -> DPI -> PrintSpace).                            |
| Fix P0 Pipeline Regressions                       | ✅ **DONE** | Argparse missing arguments resolved; `teitok_out_path` properly threaded.                     |
| Consolidate Transforms (`api_util/bbox_scale.py`) | ✅ **DONE** | Divergent logic unified; regex replaced legacy `ElementTree` logic.                           |
| `fix_teitok_bboxes.py` rewrite                    | ✅ **DONE** | Script now recalculates cleanly across directories via `dx`/`dy` shifts without parse errors. |
| Dynamic Image Extension fallback                  | ✅ **DONE** | Hardcoded `.png` assumptions removed; module captures `.jpg` and `.tiff` natively.            |

## Further Actions / Next Steps

### A — Validation & Data Guardrails
1. **XSD Schema Validation**: Build a TEITOK XML validation gate. Introduce a schema-conformance check at the end of `api_4_stats.sh` to ensure structural integrity prior to the LINDAT dataset release (Target Milestone: Q1-Q2).

### B — Upstream Alignment
2. **Relative Values Migration**: Documented as the preferred long-term direction, coordinate with the TEITOK team regarding the use of relative/resolution-independent values to eliminate fixed scaling issues entirely. 
3. **Flexiconv GUI Alignment**: Continue monitoring the requested system alignment with the Flexiconv GUI (tracked in `flexiconv/issues/1`).